// ? Calificaciones del alumno
 interface CalificacionAlumno {
    nomfase: string | null; // * Etapa
    nomentmat: string | null; // * Nombre de la materia
    calactexm: string | null; // * Calificación
    fecha_examen: string | null; // * Fecha
    numfase: string | null; // * ??
    cveentmat: string | null; // * ??
  }

  export default CalificacionAlumno;