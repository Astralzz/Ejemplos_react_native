import React from "react";
import { View } from "react-native";

interface PropsRoutes {
  ruta: string;
}

// Todo ... RUTAS GLOBALES
const Routes: React.FC<PropsRoutes> = ({ ruta }) => {
  return <View></View>;
};

export default Routes;
